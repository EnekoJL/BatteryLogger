[Battery_API]
; IP address of the Battery Management System
IP=192.168.55.193
; How often to poll the live data endpoint (seconds)
PollInterval=10
; How often to poll per-string endpoints (/api/bcs/battery/Sxx), seconds.
; Kept slower than PollInterval and fetched sequentially — the BMS already
; rate-limits (HTTP 429) under lighter load, so per-string polling every
; tick would make that worse.
StringPollInterval=30

[Logging]
; How often to write a row to the CSV file (seconds)
LogFrequency=10
; Create a new CSV file each day at midnight
RotateDaily=false

[Battery_Config]
; Number of cells in series per module (used for IR and corrected VCell calc)
NumCells=15
; Number of modules per string
NumModules=1

[Alerts]
; Log a WARNING when SOC drops below this percentage
SocLowThreshold=20
; Log a WARNING when maximum cell temperature exceeds this value (°C)
TempHighThreshold=45
